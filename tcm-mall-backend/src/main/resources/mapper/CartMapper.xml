<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcmmall.mapper.CartMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, goods_id, goods_name, goods_cover, goods_price, quantity, create_time, update_time)
        VALUES (#{userId}, #{goodsId}, #{goodsName}, #{goodsCover}, #{goodsPrice}, #{quantity}, NOW(), NOW())
    </insert>

    <update id="updateQuantity">
        UPDATE cart SET quantity = #{quantity}, update_time = NOW() WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByUserIdAndGoodsId">
        DELETE FROM cart WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </delete>

    <select id="selectById" resultType="com.tcmmall.entity.Cart">
        SELECT * FROM cart WHERE id = #{id}
    </select>

    <select id="selectByUserIdAndGoodsId" resultType="com.tcmmall.entity.Cart">
        SELECT * FROM cart WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </select>

    <select id="selectByUserId" resultType="com.tcmmall.entity.Cart">
        SELECT * FROM cart WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <delete id="deleteBatch">
        DELETE FROM cart WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>