<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcmmall.mapper.OrderDetailMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_detail (order_id, goods_id, goods_name, goods_cover, goods_price, quantity, subtotal)
        VALUES (#{orderId}, #{goodsId}, #{goodsName}, #{goodsCover}, #{goodsPrice}, #{quantity}, #{subtotal})
    </insert>

    <insert id="batchInsert">
        INSERT INTO order_detail (order_id, goods_id, goods_name, goods_cover, goods_price, quantity, subtotal) VALUES
        <foreach collection="details" item="d" separator=",">
            (#{d.orderId}, #{d.goodsId}, #{d.goodsName}, #{d.goodsCover}, #{d.goodsPrice}, #{d.quantity}, #{d.subtotal})
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="com.tcmmall.entity.OrderDetail">
        SELECT * FROM order_detail WHERE order_id = #{orderId}
    </select>
</mapper>