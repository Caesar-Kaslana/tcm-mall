<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcmmall.mapper.RoleMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (name, code, description, status, create_time, update_time)
        VALUES (#{name}, #{code}, #{description}, #{status}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE role
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM role WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.tcmmall.entity.Role">
        SELECT * FROM role WHERE id = #{id}
    </select>

    <select id="selectList" resultType="com.tcmmall.entity.Role">
        SELECT * FROM role WHERE status = 1
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" resultType="com.tcmmall.entity.Role">
        SELECT r.* FROM role r
        INNER JOIN user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>
</mapper>