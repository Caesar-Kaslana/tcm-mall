<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcmmall.mapper.CategoryMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, parent_id, sort, icon, status, create_time, update_time)
        VALUES (#{name}, #{parentId}, #{sort}, #{icon}, #{status}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.tcmmall.entity.Category">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="selectList" resultType="com.tcmmall.entity.Category">
        SELECT * FROM category WHERE status = 1
        <if test="parentId != null">AND parent_id = #{parentId}</if>
        ORDER BY sort ASC
    </select>

    <select id="selectAll" resultType="com.tcmmall.entity.Category">
        SELECT * FROM category WHERE status = 1 ORDER BY sort ASC
    </select>
</mapper>