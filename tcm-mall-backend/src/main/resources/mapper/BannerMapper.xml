<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcmmall.mapper.BannerMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO banner (title, image, link_url, sort, status, create_time, update_time)
        VALUES (#{title}, #{image}, #{linkUrl}, #{sort}, #{status}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE banner
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="image != null">image = #{image},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM banner WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.tcmmall.entity.Banner">
        SELECT * FROM banner WHERE id = #{id}
    </select>

    <select id="selectList" resultType="com.tcmmall.entity.Banner">
        SELECT * FROM banner WHERE 1=1
        <if test="status != null">AND status = #{status}</if>
        ORDER BY sort ASC
    </select>
</mapper>
